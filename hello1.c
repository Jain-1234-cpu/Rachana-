#include<stdio.h>
int main()
{
    int x=0;
    printf("Enter the value of x:");
    scanf("%d",&x);
    if(x>0)
    {
      printf("x is positive number%d",x);
    }
    else
    {
    printf("x is negative number%d",x);
    }
    return 0;

    
}